---
export const prerender = true;

import BaseLayout from '@/layouts/BaseLayout.astro';
import ProductCard from '@/components/ProductCard.astro';
import { products } from '@/data/products';
---

<BaseLayout title="Home" description="Carlu's Shop – UvsU Shaker & Laufweste. Direkt vom Hersteller. Versand in 2–4 Werktagen nach DE, AT, CH.">
  <!-- Hero -->
  <section class="relative text-bg overflow-hidden" style="background-color: #020509;">
    <!-- Cinematic vest photo — cutout PNGs with a transparent background,
         so the section's own solid bg color shows through with zero visible
         seam, positioned toward the right so it clears the text column.
         Spins once on page load (not a continuous loop) via .vest-spin. -->
    <div
      class="hidden lg:block absolute inset-y-0 right-0 xl:right-6 w-[38%]"
      style="-webkit-mask-image: linear-gradient(to left, black 70%, transparent 100%); mask-image: linear-gradient(to left, black 70%, transparent 100%);"
    >
      <div class="vest-spin-wrap w-full h-full">
        <div class="vest-spin">
          <img
            src="/images/products/laufweste-cutout-front.png"
            alt="Carlu's Laufweste, freigestellt"
            class="vest-face"
          />
          <img
            src="/images/products/laufweste-cutout-back.png"
            alt=""
            aria-hidden="true"
            class="vest-face vest-face-back"
          />
        </div>
      </div>
    </div>
    <div class="grain-overlay"></div>

    <div class="relative max-w-6xl mx-auto px-4 sm:px-6 py-16 sm:py-32 lg:py-56">
      <div class="flex flex-col-reverse sm:flex-row sm:items-center gap-6 sm:gap-8 lg:block">
        <div class="flex-1 min-w-0 lg:max-w-xl">
          <p class="mono-tag text-bg/50 mb-2 sm:mb-4 fade-in">— Carlu's Shop</p>
          <h1 class="headline text-4xl sm:text-6xl lg:text-7xl xl:text-8xl fade-in" style="animation-delay: 60ms">
            Wir liefern die Produkte,<br />
            Du lieferst <span class="text-accent">ab</span>!
          </h1>
          <p class="hidden sm:block mt-4 sm:mt-8 text-xs sm:text-lg lg:text-xl text-bg/70 fade-in" style="animation-delay: 120ms">
            Qualitätsprodukte, die dir deinen Fitness-Alltag erleichtern.<br />
            Deutsche Marke. Versand nach Deutschland, Österreich, Schweiz.
          </p>
          <a
            href="/produkte"
            class="sticker mt-5 sm:mt-10 w-full sm:w-auto flex sm:inline-flex items-center justify-center gap-1.5 sm:gap-2 bg-accent text-accent-ink font-bold px-4 py-2.5 sm:px-8 sm:py-4 rounded-sm text-sm sm:text-lg fade-in"
            style="animation-delay: 180ms"
          >
            Alle Produkte
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
          </a>
        </div>

        <!-- Mobile/tablet: image next to the text instead of the
             side-panel treatment (which only works at lg+ widths) -->
        <div class="lg:hidden shrink-0 w-full max-w-[280px] mx-auto sm:w-[52%] sm:max-w-none sm:mx-0 fade-in" style="animation-delay: 220ms">
          <div class="vest-spin-wrap" style="aspect-ratio: 880 / 1113;">
            <div class="vest-spin">
              <img
                src="/images/products/laufweste-cutout-front.png"
                alt="Carlu's Laufweste, freigestellt"
                class="vest-face"
              />
              <img
                src="/images/products/laufweste-cutout-back.png"
                alt=""
                aria-hidden="true"
                class="vest-face vest-face-back"
              />
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="spec-rule"></div>
  </section>

  <!-- Products -->
  <section class="max-w-6xl mx-auto px-4 sm:px-6 py-16">
    <div class="flex items-end justify-between mb-8 reveal">
      <div>
        <p class="mono-tag mb-1">Kollektion</p>
        <h2 class="text-2xl sm:text-3xl font-bold">Unsere Produkte</h2>
      </div>
      <a href="/produkte" class="text-sm font-medium underline underline-offset-4 hover:text-accent transition-colors">Alle ansehen</a>
    </div>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {products.map((product, i) => (
        <div class="reveal" style={`transition-delay: ${i * 80}ms`}>
          <ProductCard product={product} />
        </div>
      ))}
    </div>
  </section>

  <!-- USP -->
  <section class="spec-rule">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 pt-12 pb-16 grid grid-cols-1 sm:grid-cols-3 gap-8">
      <div class="reveal">
        <p class="mono-tag mb-2 text-accent">01 / Qualität</p>
        <h3 class="font-bold text-lg mb-1">Geprüfte Materialien</h3>
        <p class="text-sm text-muted">Jedes Produkt wird sorgfältig auf Qualität und Langlebigkeit geprüft.</p>
      </div>
      <div class="reveal" style="transition-delay: 100ms">
        <p class="mono-tag mb-2 text-teal">02 / Lieferung</p>
        <h3 class="font-bold text-lg mb-1">Schneller Versand</h3>
        <p class="text-sm text-muted">Versand in 2–4 Werktagen nach Deutschland, Österreich und der Schweiz.</p>
      </div>
      <div class="reveal" style="transition-delay: 200ms">
        <p class="mono-tag mb-2 text-accent">03 / Service</p>
        <h3 class="font-bold text-lg mb-1">14 Tage Rückgabe</h3>
        <p class="text-sm text-muted">Gesetzliches Widerrufsrecht: 14 Tage ohne Angabe von Gründen.</p>
      </div>
    </div>
  </section>
</BaseLayout>
